# PC02b - Hyper-Paremeter Optimization for a CNN Architecture

Your main task is to go run the `CNNHPO.ipynb` notebook, which will create a file `_best_cnn_optuna.pth` file. Make sure that you add that file to your repo, since that is the one that we will use for grading to make sure that optimization was run correctly. I recomment you check in github.com to ensure that the file is there after you push your repo.

It is encouraged that you go over the details of this script since you may need something like this for the competition.
